{
  "name": "@homelab-manager/agent-updater",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage 2>&1 | node ../scripts/check-coverage.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dockerode": "^5.0.0"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/bun": "^1.3.10",
    "typescript": "^7.0.2"
  }
}
